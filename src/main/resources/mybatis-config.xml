<?xml version="1.0" encoding="utf-8"?>
 <!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="applications.properties" />
	<typeAliases>
		<package name="com.briup.estore.bean" />
	</typeAliases>
	<!-- <typeHandlers> <typeHandler handler="com.briup.mybatis.day1.PhoneTypeHandler" 
		/> </typeHandlers> -->
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driverClassName}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="mappers/CustomerMapper.xml" />
		<mapper resource="mappers/BookMapper.xml" />
		<mapper resource="mappers/CategoryMapper.xml" />
		<mapper resource="mappers/ShopCarMapper.xml" />
		<mapper resource="mappers/OrderMapper.xml" />
	</mappers>
</configuration>